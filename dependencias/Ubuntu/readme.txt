Para instalar o GLUT no Ubuntu:

"sudo apt-get install freeglut3 freeglut3-dev"

Se seu Ubuntu for >= 11.10 você ainda deve
"sudo apt-get install binutils-gold"

Dependendo tem que relinkar os *.so do GLUT necessários na pasta /usr/lib